[tools]
	bun  = "1.4.2"
	node = "22.23.2"

[tasks.dev]
	run = "bun dev"
[tasks.build]
	run = "bun run build"
[tasks.preview]
	run = "bun preview"

[env]
	_.path = [
		'{{config_root}}/node_modules/.bin',
	]
