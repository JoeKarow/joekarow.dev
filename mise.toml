[tools]
	bun  = "1.2.21"
	node = "22.19.0"

[tasks.dev]
	run = "bun dev"
[tasks.build]
	run = "bun run build"
[tasks.preview]
	run = "bun preview"

[env]
	_.path = [
		'{{config_root}}/node_modules/.bin',
	]
