<ul class="icons">
    {% for item in social.socials %}
        {% if item.enabled %}
        <li><a href="{{ item.url }}" class="{{ item.iconClass }}"><span class="label">{{ item.name }}</span></a></li>
        {% endif %}
    {% endfor %}
</ul>